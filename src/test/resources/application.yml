spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create      # springboot의 디폴트값은 create-drop
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

# main과 test는 설정을 따로 가져가는 것이 맞다
# 위와 같이 주석처리 할 시 부트가 자동으로 메모리 db모드로 실행한다